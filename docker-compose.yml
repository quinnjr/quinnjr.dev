version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: quinnjr.tech:latest
    container_name: quinnjr-tech
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000
    restart: unless-stopped

