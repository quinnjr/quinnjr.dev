echo "🔧 Generating Prisma client..."
pnpm prisma:generate || exit 1

echo "🔍 Running linter..."
pnpm lint || exit 1

echo "🧪 Running server tests..."
pnpm test:server || {
  echo "⚠️  Tests failed, but continuing commit..."
  echo "Please fix test failures before pushing to remote."
  exit 0
}

echo "✅ Pre-commit checks passed!"
